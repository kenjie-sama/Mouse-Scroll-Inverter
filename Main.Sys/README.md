# Mouse-Scroll-Inverter
Run the Powershell as Administrator and locate the file 
Execute the following command to invert the mouse scroll direction:
```powershell -ExecutionPolicy Bypass -File "app.ps1"
This will run the script and invert the mouse scroll direction. You can run the script again to revert back to the original scroll direction.